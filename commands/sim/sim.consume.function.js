// var tradesService = require('./_services/trades-service')

// module.exports = function (conf) {

// tradesServiceInstance = tradesService(conf)

// return (mostRecentlyProcessedTradeId, queue, cb) => {
// var cursor, reversing, reverse_point
// if (!optsObj.min_periods) optsObj.min_periods = 1
// var query_start = optsObj.start ? tb(optsObj.start).resize(optsObj.period_length).subtract(optsObj.min_periods + 2).toMilliseconds() : null

// }
// }